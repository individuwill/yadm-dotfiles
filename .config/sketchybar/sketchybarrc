# This is a demo config to showcase some of the most important commands.
# It is meant to be changed and configured, as it is intentionally kept sparse.
# For a (much) more advanced configuration example see my dotfiles:
# https://github.com/FelixKratz/dotfiles

PLUGIN_DIR="$CONFIG_DIR/plugins"

# Can print out the symbols with
# `printf '%s %s %s\n' $'\uf2d0' $'\uf009' $'\uf013'`
ICON_WINDOW=''   # window-maximize \uf2d0
ICON_LAYOUT=''  # th-large \uf009
ICON_MODE=''     # cog \uf013


##### Bar Appearance #####
# Configuring the general appearance of the bar.
# These are only some of the options available. For all options see:
# https://felixkratz.github.io/SketchyBar/config/bar
# If you are looking for other colors, see the color picker:
# https://felixkratz.github.io/SketchyBar/config/tricks#color-picker

sketchybar --bar position=bottom \
                 height=40 \
                 blur_radius=30 \
                 color=0x40000000 \
                 margin=5 \
                 corner_radius=10 \
                 shadow=on \
                 border_width=1 \
                 border_color=0x22ffffff \
                 padding_left=8 \
                 padding_right=8 \
                 topmost=window

##### Changing Defaults #####
# We now change some default values, which are applied to all further items.
# For a full list of all available item properties see:
# https://felixkratz.github.io/SketchyBar/config/items


default=(
  padding_left=5
  padding_right=5
  icon.font="FiraCode Nerd Font:Bold:17.0"
  label.font="FiraCode Nerd Font:Bold:14.0"
  icon.color=0xffffffff
  label.color=0xffffffff
  icon.padding_left=4
  icon.padding_right=4
  label.padding_left=4
  label.padding_right=4
)
sketchybar --default "${default[@]}"

##### Adding Left Items #####
# We add some regular items to the left side of the bar, where
# only the properties deviating from the current defaults need to be set

sketchybar --add item chevron left \
           --set chevron icon= label.drawing=off \
           --add item front_app left \
           --set front_app icon.drawing=off script="$PLUGIN_DIR/front_app.sh" \
           --subscribe front_app front_app_switched

##### Adding Right Items #####
# In the same way as the left items we can add items to the right side.
# Additional position (e.g. center) are available, see:
# https://felixkratz.github.io/SketchyBar/config/items#adding-items-to-sketchybar

# Some items refresh on a fixed cycle, e.g. the clock runs its script once
# every 10s. Other items respond to events they subscribe to, e.g. the
# volume.sh script is only executed once an actual change in system audio
# volume is registered. More info about the event system can be found here:
# https://felixkratz.github.io/SketchyBar/config/events

sketchybar --add item clock right \
           --set clock update_freq=10 icon=  script="$PLUGIN_DIR/clock.sh" \
             click_script="sketchybar --set clock popup.drawing=toggle" \
             popup.background.border_width=2 \
             popup.background.corner_radius=5 \
             popup.background.border_color=0xff9dd274 \
             popup.background.color=0x77000000 \
           --add item volume right \
           --set volume script="$PLUGIN_DIR/volume.sh" \
           --subscribe volume volume_change \
           --add item battery right \
           --set battery update_freq=120 script="$PLUGIN_DIR/battery.sh" \
           --subscribe battery system_woke power_source_change

sketchybar \
           --add item clock_slc popup.clock \
           --set clock_slc icon.drawing=off script="$PLUGIN_DIR/clock.sh America/Denver" \
             update_freq=10 \
           --add item clock_lon popup.clock \
           --set clock_lon icon.drawing=off script="$PLUGIN_DIR/clock.sh Europe/London" \
             update_freq=10 \
           --add item clock_utc popup.clock \
           --set clock_utc icon.drawing=off script="$PLUGIN_DIR/clock.sh UTC" \
             update_freq=10

# turn off individual backgrounds
sketchybar --set clock  background.drawing=off \
           --set volume background.drawing=off \
           --set battery background.drawing=off

# add one shared background behind them
sketchybar --add bracket status_bracket volume battery clock \
  --set status_bracket \
    background.color=0x22ffffff \
    background.corner_radius=10 \
    background.height=26 \
    background.border_width=1 \
    background.border_color=0x22ffffff

sketchybar --add item yadm_status right \
           --set yadm_status update_freq=60 \
                      script="$CONFIG_DIR/plugins/yadm_status.sh" \
                      background.padding_left=0 \
                      label.drawing=off \
           --subscribe yadm_status mouse.clicked mouse.entered mouse.exited

# The Popup Item (The "Tooltip")
sketchybar --add item yadm_status.details popup.yadm_status \
           --set yadm_status.details label.padding_left=10 \
                              label.padding_right=10 \
                              background.corner_radius=5 \
                              background.height=25

# Detailed Tooltip Styling
sketchybar --set yadm_status.details \
           label.padding_left=12 \
           label.padding_right=12 \
           label.color=0xffffffff \
           background.color=0xee24273a \
           background.corner_radius=8 \
           background.height=26 \
           background.border_color=0xff494d64 \
           background.border_width=1 \
           background.shadow.drawing=on

# General Popup Container Styling
sketchybar --set yadm_status \
           popup.background.border_width=0 \
           popup.background.corner_radius=9 \
           popup.background.shadow.drawing=on \
           popup.blur_radius=20

sketchybar --add item lock_screen right \
           --set lock_screen \
                     icon= \
                     background.padding_left=10 \
                     label.drawing=off \
                     click_script="$CONFIG_DIR/plugins/lock_screen.sh"

sketchybar --add event aerospace_workspace_change

for sid in $(aerospace list-workspaces --all); do
    sketchybar --add item space.$sid left \
        --subscribe space.$sid aerospace_workspace_change \
        --set space.$sid \
        background.drawing=on \
        background.color=0x22ffffff \
        background.corner_radius=6 \
        background.height=26 \
        label.padding_left=8 \
        label.padding_right=8 \
        label="$sid" \
        click_script="aerospace workspace $sid" \
        script="$CONFIG_DIR/plugins/aerospace.sh $sid"
done

sketchybar --add item as_window left \
  --set as_window label=".." icon="$ICON_WINDOW" script="$PLUGIN_DIR/aerospace_window.sh" \
                 scroll_texts=on label.max_chars=50 scroll_duration=120

sketchybar --add item as_layout left \
  --set as_layout icon="$ICON_LAYOUT" label="..." script="$PLUGIN_DIR/aerospace_layout.sh"

sketchybar --add event aerospace_focus_change
sketchybar --subscribe as_window aerospace_focus_change
sketchybar --subscribe as_layout aerospace_focus_change

sketchybar --add event aerospace_layout_change
sketchybar --subscribe as_layout aerospace_layout_change
sketchybar --subscribe as_window aerospace_layout_change

sketchybar --add event aerospace_mode
sketchybar --add item as_mode left \
  --set as_mode icon="$ICON_MODE" label="main" \
  --subscribe as_mode aerospace_mode \
  --set as_mode script="$PLUGIN_DIR/aerospace_mode.sh"

# put layout + mode right after your last workspace item (e.g. space.9 or space.Z depending what you keep)
sketchybar --move as_layout after space.T
sketchybar --move as_mode after as_layout

# turn off individual backgrounds
sketchybar --set as_layout  background.drawing=off \
           --set as_mode background.drawing=off

# add one shared background behind them
sketchybar --add bracket layout_mode_bracket as_layout as_mode \
  --set layout_mode_bracket \
    background.color=0x22ffffff \
    background.corner_radius=10 \
    background.height=26 \
    background.border_width=1 \
    background.border_color=0x22ffffff

##### Force all scripts to run the first time (never do this in a script) #####
sketchybar --update

# Wait 7 days (10080 minutes) before installing newly published versions
minimumReleaseAge=10080

# Optional: exclude packages you trust or need on the bleeding edge
# minimumReleaseAgeExclude=my-internal-pkg,@myorg/shared

# To bypass if needed:
# pnpm add some-package --config.minimumReleaseAge=0

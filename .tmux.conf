##### Core behavior #####

# Use vi keys in copy mode
set -g mode-keys vi

# Faster escape from prefix
set -s escape-time 0

# Increase scrollback history
set -g history-limit 250000

# Enable true color (important on macOS terminals)
set -g default-terminal "tmux-256color"
set -as terminal-features ",*:RGB"

# Mouse support (scrolling, resizing panes, selecting windows)
set -g mouse on

# Silence warnings from pi during startup
set -g extended-keys on
set -as terminal-features 'xterm*:extkeys'
set -g extended-keys-format csi-u

# Give claude want it wants
set -g focus-events on
set -g allow-passthrough on

##### Prefix #####

# Keep default prefix (Ctrl-b)
# If you ever want Ctrl-a later, you can switch cleanly


##### Pane splitting (Vim-style) #####

# Split panes using | and -
bind | split-window -h
bind - split-window -v

# Vim-like pane navigation
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Resize panes with repeatable keys
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5


##### Windows #####

# Start window numbering at 1 (more human-friendly)
set -g base-index 1
set -g pane-base-index 1

# Automatically renumber windows
set -g renumber-windows on

# Quickly switch windows
# Disabled as they conflict with aerospace
# bind -n M-1 select-window -t 1
# bind -n M-2 select-window -t 2
# bind -n M-3 select-window -t 3
# bind -n M-4 select-window -t 4
# bind -n M-5 select-window -t 5


##### Copy mode (Vim-like) #####

# Enter copy mode with v
bind v copy-mode

# Vim-style selection
bind -T copy-mode-vi v send -X begin-selection
# this one is only needed for non-mac specific use
# bind -T copy-mode-vi y send -X copy-selection

# macOS clipboard integration
# (requires pbcopy, which macOS has by default)
bind -T copy-mode-vi y send -X copy-pipe-and-cancel "pbcopy"

##### Quality-of-life #####

# Reload config
bind r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded"

# Visual bell instead of audible
# set -g bell-action none

# Add on May 9 2026 for claude to notify:
##### Attention indicators #####
set -g monitor-bell on
set -g monitor-activity off
set -g bell-action other          # bells from non-current windows flag
set -g activity-action other
set -g visual-bell off            # silent flag, no screen flash
set -g visual-activity off

##### Minimal status bar #####

set -g status on
set -g status-position bottom
set -g status-interval 60

# Colors (dark, flat)
set -g status-style "bg=colour235,fg=colour250"

# Left: session name only
set -g status-left-length 40
set -g status-left "#[fg=colour81] #S "
set -g status-left "#[fg=colour81] #S #{prefix_highlight} "

# Right: empty (SketchyBar handles system info)
set -g status-right ""
set -g status-right-length 0

# Windows
set -g window-status-style "bg=colour235,fg=colour245"
set -g window-status-current-style "bg=colour238,fg=colour81"
set -g window-style "fg=colour245,bg=colour237"          # inactive: faded
set -g window-active-style "fg=colour253,bg=colour232"   # active: dark, sharp

# Change on May 9 2026 for claude status stuff
# set -g window-status-format "  #I:#W  "
# set -g window-status-current-format "  #[bold]#I:#W  "
set -g window-status-format "  #I:#W#{?window_bell_flag,!,}  "
set -g window-status-current-format "  #[bold]#I:#W  "
set -g window-status-bell-style "bg=colour235,fg=colour226,bold"
set -g window-status-activity-style "bg=colour235,fg=colour222"

# Pane borders
set -g pane-border-style "fg=colour238"
set -g pane-active-border-style "fg=#fe8019,bold"
set -g pane-border-lines heavy   # options: single, double, heavy, simple, number

##### Plugins #####

# Auto-install TPM if missing
if-shell 'command -v git >/dev/null 2>&1 && [ ! -d ~/.tmux/plugins/tpm ]' \
  'run-shell "git clone --depth=1 https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm"'

# Plugin list
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-sessionist'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'tmux-plugins/tmux-yank'

# Continuum config
set -g @continuum-save-interval '15'
set -g @continuum-restore 'on'

# Prefix highlight config (optional but nice)
set -g @prefix_highlight_show_copy_mode 'on'
set -g @prefix_highlight_output_prefix ' ⌨ '
set -g @prefix_highlight_output_suffix ' '

set -g @tpm_plugins_install 'on'
# Initialize TPM (must be last)
run '~/.tmux/plugins/tpm/tpm'
